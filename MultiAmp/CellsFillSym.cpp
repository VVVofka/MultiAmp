#include "pch.h"
#include "CellsFillSym.h"

std::array<Direct, 16>& CellsFillSym::fill(size_t mask, const std::array<Direct, 16>& v_in){
	switch(mask){
	case 0:

		break;
	default:
		break;
	}
    return vout;
} // ///////////////////////////////////////////////////////////////////////////
void CellsFillSym::w0(const std::array<Direct, 16>& v_in){
	size_t act = 0;
	Direct dirAct(v_in[act]);
} // ///////////////////////////////////////////////////////////////////////////
