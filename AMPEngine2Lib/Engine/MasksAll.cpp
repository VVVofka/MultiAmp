#include "MasksAll.h"

MasksAll::MasksAll(const structAll* cfg_all, accelerator_view* m_accl_view) :
	a(cfg_all, m_accl_view),
	f(cfg_all, m_accl_view)
{
	//
} // ///////////////////////////////////////////////////////////////////////////////////////////
